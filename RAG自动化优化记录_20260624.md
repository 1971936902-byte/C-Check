# RAG 自动化优化记录 2026-06-24

本次围绕 RAG 模块最值得优先补齐的 7 个点做了实现和验证。

## 1. clangd/libclang 语义入口增强

- libclang 解析时自动读取 `compile_commands.json`。
- 支持提取 `-I`、`-D`、`-U`、`-isystem`、`-std` 等编译参数。
- 目标是让 include path、宏定义、类型解析、声明/定义跳转具备真实项目上下文。

## 2. Qdrant 与 embedding 接入主检索链路

- 新增远程 embedding 配置，支持 OpenAI-compatible `/v1/embeddings`。
- 无远程 embedding 服务时自动回退本地 hashing embedding。
- Qdrant upsert 前自动确保 collection 存在。
- 检索器将 Qdrant 命中作为候选证据之一参与二次排序和预算裁剪。

## 3. 金标评估集补齐

- 在现有 fixture 基础上新增覆盖回调、函数级审查单元、Qdrant 命中、跨任务缓存、诊断输出的测试。
- 当前仍是轻量金标/自动金标阶段，后续可继续接入人工标注真实项目样本。

## 4. 持久化跨任务解析缓存

- 新增 `code_parse_cache` 表。
- 使用 `content_hash + parser_version + settings_hash` 复用解析结果。
- 重复上传或跨任务相同文件可跳过符号解析，降低索引耗时。

## 5. 函数级审查单元进入模型下发链路

- 模型调用前可将 RAG 规划出的函数审查单元转换为临时审查输入。
- 保留真实 `relative_path`，避免报告定位变成虚拟路径。
- 单元内容包含函数名、文件、行号和函数代码片段。

## 6. 函数指针、回调、结构体函数表识别增强

- 解析 `.field = function_name` 风格的结构体回调绑定。
- 图谱新增 `CALLBACK_BINDING_TARGETS_FUNCTION` 关系。
- 可覆盖驱动代码中常见的 ops 表、回调表、函数指针初始化模式。

## 7. RAG 可观测性

- 新增 `/api/code-index/{task_id}/observability`。
- 返回索引统计、候选证据数量、选中证据、被裁剪证据、bucket 分布、token/字符预算等。
- 便于定位召回不足、Top-K 噪声过高、Qdrant 命中缺失等问题。

## 验证结果

| 验证项 | 结果 |
| --- | --- |
| 静态编译检查 | 通过 |
| RAG 专项测试 | `25 passed` |
| 后端完整测试 | `154 passed, 3 warnings` |
| Recall@10 | 1.0000 |
| Precision@10 | 0.5000 |
| MRR | 1.0000 |
| NDCG@10 | 1.0000 |
| Token Waste Ratio | 0.5000 |
| 跨任务缓存首次构建 | 0 hit / 4 miss |
| 跨任务缓存二次构建 | 4 hit / 0 miss |
| 检索延迟 P50 | 5.61 ms |
| 检索延迟 P95 | 11.09 ms |
| 检索延迟 P99 | 13.51 ms |

## 剩余风险

- 远程 embedding、Qdrant、真实云端模型链路需要在服务器环境进行联调。
- 人工金标集仍需继续扩展真实项目样本。
- libclang 的能力依赖服务器是否安装 clang/libclang 运行库。
