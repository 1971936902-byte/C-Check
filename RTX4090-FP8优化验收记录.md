# RTX 4090 48G FP8 优化验收记录

测试日期：2026-07-01
测试样本：`imgRead.c`、`imgRead_libfuzzer.c`、`imgRead_socket.c`

## 部署配置

- 审核模型：`RedHatAI/Qwen2.5-Coder-14B-Instruct-FP8-dynamic`
- 嵌入模型：Jina Code Embeddings 0.5B，vLLM 在线 FP8
- Qwen：FP8 权重、FP8 KV Cache、FlashInfer、CUDA Graph、Prefix Cache、n-gram 推测解码
- RAG：批量 32、896 维、Qdrant 独立集合、禁止哈希降级
- 调度：Celery 单并发，避免两个模型同时争抢 GPU

## 问题与修改

第二阶段的严格 JSON Schema 包含字符串长度、数组数量等约束，vLLM 0.8.5 无法使用 xgrammar，自动回退 Outlines 后请求无法完成。故障基线耗时 398.549 秒，5 个格式化批次全部失败并走后端回退。

第二阶段本质是候选记录的类型映射、字段规范化和规则过滤，不需要再次推理漏洞。新增 `CANDIDATE_FORMAT_MODEL_ENABLED` 开关；生产环境关闭二次模型改写，使用确定性解析和现有证据过滤器完成收口。模型格式化路径仍保留，可用于对照实验。

## 代码修改点

- `backend/app/core/config.py`
  - Jina 默认嵌入批量从 16 调整为 32，减少串行请求开销。
  - 新增 `candidate_format_model_enabled`，允许生产环境选择确定性第二阶段，同时保留 LLM 格式化实验路径。
- `backend/app/services/model_router.py`
  - 关闭模型格式化时，直接解析第一阶段 JSONL，再交给原有类别过滤、源码锚定和证据过滤逻辑。
  - 本地规范化成功不计失败批次，避免把可恢复的模型输出误记为第二阶段失败。
- `backend/alembic/versions/0014_merge_embedding_and_candidate_heads.py`
  - 合并代码解析缓存与候选 JSONL 两条 Alembic 分支，消除多 migration heads 导致的部署升级失败。
- `deploy/native/c-check-rag.env`
  - 固定 Jina OpenAI-compatible 服务、896 维向量、批量 32、Qdrant 独立集合并禁止哈希静默降级。
  - 关闭不兼容的严格 structured outputs 和第二阶段模型改写。
- `deploy/native/systemd/`
  - 增加 Qdrant、Jina FP8、Qwen FP8 systemd 单元以及 API/Worker 的 RAG 环境注入 drop-in。
  - Qwen 启用 FP8 KV Cache、FlashInfer、CUDA Graph、Prefix Cache 和 n-gram 推测解码。

## 优化收益与取舍

- Jina 批量 32：提高索引吞吐，避免逐条编码；显存占用仍受独立 vLLM 服务限制。
- Qwen FP8 与 CUDA Graph：降低权重和 KV Cache 显存，提升重复审核解码吞吐。
- 确定性第二阶段：消除 Outlines 卡死、额外生成耗时和模型对候选事实的二次改写；代价是第二阶段不再承担语义复核，漏洞真实性继续由第一阶段及源码证据过滤器负责。
- 单 Worker 调度：当前以任务级串行避免双模型争抢 GPU；如未来扩展多 Worker，应增加 Redis 分布式 GPU 锁。

## 部署记录

1. 检查 RTX 4090 48G、驱动、CUDA、磁盘和端口，开启 persistence mode。
2. 部署 MySQL、Redis、Nginx、Qdrant、后端 API、Celery Worker 和前端静态资源。
3. 使用独立 vLLM 环境 `/opt/vllm-cu124`，安装 vLLM 0.8.5.post1、PyTorch CUDA 12.4、FlashInfer 和 Ninja。
4. 部署 Jina 0.5B 嵌入服务，使用在线 FP8、CUDA Graph、批量请求和独立显存配额。
5. 部署 `RedHatAI/Qwen2.5-Coder-14B-Instruct-FP8-dynamic`，确认日志中的量化类型为 `compressed-tensors`，并启用 FP8 KV Cache。
6. 通过 systemd drop-in 把 `/etc/c-check/c-check-rag.env` 同时注入 API 和 Worker，避免只修改交互式 Shell 环境却未影响生产进程。
7. 执行 Alembic 升级、模型节点登记和三文件端到端审核，再进行前端、后端、GPU、Qdrant 与 Xid 检查。

生产部署使用的主要服务：

| 服务 | 监听/用途 |
| --- | --- |
| `c-check-vllm-qwen` | `127.0.0.1:8001`，第一阶段代码审核 |
| `c-check-vllm-jina` | `127.0.0.1:8002`，代码向量编码 |
| `qdrant` | `127.0.0.1:6333`，向量索引 |
| `c-check-api` | 后端 API 与前端衔接 |
| `c-check-worker` | 单并发审核任务执行 |

## 踩坑与解决办法

### 1. Alembic 出现两个 heads

- 现象：生产升级无法确定唯一 head。
- 原因：代码解析缓存和候选 JSONL 功能分别创建了迁移分支。
- 解决：新增 merge migration，同时依赖两个 revision。
- 经验：合并长期功能分支前执行 `alembic heads`；CI 应断言只存在一个 head。

### 2. Alembic version 字段长度不足

- 现象：较长 revision 写入 MySQL `alembic_version` 时失败。
- 原因：数据库字段仍是默认较短长度。
- 解决：生产库将字段扩展为足够长度后再升级。
- 经验：revision 名应保持简短；部署检查应验证版本字段宽度。

### 3. Triton JIT 找不到 `libcuda.so`

- 现象：首次真实推理返回 500，Triton 链接阶段提示无法找到 `-lcuda`。
- 原因：系统只有 `libcuda.so.1`，缺少开发期链接名 `libcuda.so`。
- 解决：补齐指向驱动库的系统符号链接，再重启 Qwen 服务。
- 经验：模型健康检查返回 200 不代表推理内核已编译；部署验收必须包含一次真实生成。

### 4. FlashInfer 首次加载缺少 Ninja

- 现象：FlashInfer JIT 后端初始化失败或退化。
- 原因：运行环境未安装 Ninja 构建工具。
- 解决：安装 Ninja 后重启，日志确认 `Using FlashInfer backend` 和 JIT kernel 加载完成。
- 经验：应同时检查服务参数、启动日志和实际吞吐，不能只看进程存活。

### 5. 第二阶段 Outlines 卡死

- 现象：第一阶段约 36 秒完成，但第二阶段停留数分钟；vLLM 显示请求已不占 GPU，HTTP 却未返回。
- 原因：Pydantic JSON Schema 含 `maxLength`、`maxItems` 等约束，xgrammar 不支持后自动回退 Outlines，在当前 vLLM/模型组合下卡死。
- 解决：生产环境关闭 strict structured outputs，并最终改用确定性第二阶段规范化。
- 经验：约束解码需要对目标 vLLM 版本做兼容性测试；格式转换类工作优先使用确定性代码。

### 6. 盲目放大第二阶段批次会损失召回

- 现象：将 38 条候选放入一个批次后，总耗时降至 38 秒，但最终只返回 6 条。
- 原因：模型在大批量格式化时提前结束或遗漏记录。
- 解决：不以牺牲召回换取速度，改为逐条确定性解析；最终 41 条全部进入格式化，失败批次为 0。
- 经验：性能验收必须同时查看耗时、候选数、最终命中和失败批次，不能只优化延迟。

### 7. systemd 环境与交互式 Shell 不一致

- 现象：Shell 中实例化 Settings 仍显示 hashing 默认值，容易误判生产配置未生效。
- 原因：RAG 变量通过 systemd `EnvironmentFile` 注入，仅存在于服务进程环境。
- 解决：读取 `/proc/<MainPID>/environ` 验证 API/Worker 实际环境。
- 经验：配置验收应面向目标进程，不以当前 SSH Shell 为准。

### 8. Celery warm shutdown 偶发卡住

- 现象：Worker 重启长时间处于 `deactivating`。
- 原因：旧 Worker 正等待模型 HTTP 请求或任务清理。
- 解决：先确认没有需要保留的活跃任务，再终止旧进程并以单并发重新启动。
- 经验：生产升级前先排空队列；不要在任务执行中直接强制重启。

## 可复用经验

- 模型文件名带 FP8 不等于运行时已启用 FP8，应以 vLLM 的量化识别日志和显存占用为准。
- Jina 与 Qwen 共卡时，先限制各自显存，再采用业务串行调度；单纯依赖 `gpu-memory-utilization` 不能解决算力争抢。
- Prefix Cache 只有固定提示词前缀稳定时才有收益，应把固定 system prompt 放在变化源码之前。
- RAG 换嵌入模型或维度时必须使用新 Qdrant 集合，禁止在旧集合混写不同向量空间。
- 禁止生产环境静默回退 hashing，否则服务看似成功但检索质量不可比。
- 每次性能实验同时记录模型版本、服务参数、任务 ID、候选数、最终数、失败批次、显存和 Xid，才能复现与横向比较。

## 吞吐量专项优化实验

### 优化思路总结

本轮先把“单请求解码速度”和“系统聚合吞吐”分开测量，避免用增加chunk、扩大上下文或提高输出上限掩盖真实瓶颈。固定512个输出token建立可重复基线后，依次验证V1引擎、双序列批处理、多步调度、任务拆分和n-gram推测解码。

实验表明，`max_tokens`只控制输出上限，增加chunk主要增加prefill和上下文成本，都不能提升单序列解码速度。RTX4090上的14B单序列解码主要受权重读取和执行内核限制。JSONL输出包含大量重复键名、标点和结构，适合使用无草稿模型的n-gram推测解码；目标模型仍逐token校验推测结果，因此不改变接口协议和后端过滤流程。

最终选择“完整上下文单任务 + n-gram推测解码”，而没有采用单任务切块或默认双任务并发。选择标准不是只看tok/s，而是同时满足端到端耗时、第一阶段候选、最终命中、失败批次、显存和服务稳定性。

固定提示词、`max_tokens=512`、强制生成满512 token的微基准结果：

| 方案 | 热态单请求吞吐 | 结论 |
| --- | ---: | --- |
| FP8 KV + vLLM V0 | 50.5 tok/s | 原始稳定基线 |
| BF16/auto KV + vLLM V1 | 49.8 tok/s | 慢约1.4%，不采用 |
| FP8 KV + V0 + 双序列 | 聚合99.9 tok/s | 总吞吐翻倍，但业务并发需验证召回 |
| FP8 KV + V0 + n-gram推测解码 | 约109 tok/s | 单请求提升约116%，采用 |

n-gram配置为5个推测token、2–4 token提示词窗口。该方法复用JSONL输出中重复出现的键名和结构，不加载额外草稿模型。vLLM 0.8.5的推测解码与Chunked Prefill不兼容，因此关闭Chunked Prefill，并将`max-num-batched-tokens`从4096调整为8192以覆盖完整上下文。

三次完整三文件审核结果：

| 任务 ID | 服务端耗时 | 第一阶段候选 | 最终漏洞 | 失败批次 |
| --- | ---: | ---: | ---: | ---: |
| `15570120-8048-4380-8556-729392dcb627` | 16.127秒 | 37 | 28 | 0 |
| `12860f35-55ee-48b6-9e99-b8b5148949d2` | 14.072秒 | 31 | 25 | 0 |
| `1ba61daa-60cb-4ee7-a62f-2f4a4faf1982` | 16.242秒 | 44 | 36 | 0 |

中位耗时16.127秒，相比优化前31.192秒下降48.3%；候选中位数37，与既有实验的波动区间一致。

未采用的实验：

- `num-scheduler-steps=8`：V0 + FlashInfer触发`block_tables`形状错误并导致引擎退出。
- 单任务拆成两个并发审核单元：耗时降至28.34秒，但最终漏洞由34降到24，召回损失不可接受。
- 同节点双完整任务并发：不同源码哈希时总吞吐接近翻倍，但候选输出存在批处理数值波动，暂不作为默认生产策略。

## 最终结果

| 指标 | 结果 |
| --- | ---: |
| 任务 ID | `271aee7f-82b2-4797-96fa-44a99518a5aa` |
| 服务端耗时 | 31.192 秒 |
| 端到端墙钟 | 32.313 秒 |
| 第一阶段候选 | 41 |
| 第二阶段格式化 | 41 |
| 证据过滤剔除 | 7 |
| 最终漏洞 | 34 |
| JSON 失败批次 | 0 |
| GPU 显存 | 39096 / 49140 MiB |
| Qdrant 状态/点数 | green / 308 |
| Xid | 0 |

相对 398.549 秒故障基线，服务端耗时下降约 92.2%；达到完整审核不超过 45 秒、理想 30–40 秒、失败批次为 0、显存不超过 43GB 的验收目标。

## 回归检查

- 前端：7 个测试文件、41 个测试全部通过。
- 后端相关测试：92 通过；1 个配置默认值测试受生产环境 `MODEL_MAX_TOKENS=1024` 注入影响，断言期望源码默认值 2048，不属于本次代码回归。
- 48 小时稳定性属于持续观察项，当前仅完成单轮负载与 Xid 检查。
