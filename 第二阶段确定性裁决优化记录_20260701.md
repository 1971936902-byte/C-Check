# 第二阶段确定性裁决优化记录（2026-07-01）

## 修改内容

- 第一阶段 JSONL 由后端完整解析和规范化，短字段 `c` 保存原始类别。
- 已匹配用户所选类别的候选由后端直接生成最终结果，不再调用格式化模型。
- 仅将 `other` 或类别不匹配候选交给语义模型，模型只返回候选编号、`drop/correct` 和类别。
- 语义批次最多 12 条，输出预算按批次动态设置为 128–384 tokens。
- 模型调用前对候选去重；同一漏洞位于不同代码行时仍分别保留。
- 源码锚定和规则过滤前移到语义分流之前，明显无效候选不再消耗模型推理。
- 移除跨文件、跨函数的全局锚点漂移，注释行优先定位到邻近有效语句。
- 性能、可维护性、兼容性、可移植性等用户已选择类别不再被旧过滤器删除。
- 最终严格报告 JSON 始终由后端基于原始候选与语义裁决合并生成。
- 日志分别记录 RAG、第一阶段模型、格式化、后端验证、语义裁决、合并和总耗时。

## 验证结果

- 针对性测试：`64 passed`。
- 完整后端测试：`209 passed`，仅有 3 条既有框架弃用警告。
- 静态编译与 `git diff --check`：通过。
- API、Worker、Qwen、Jina、Qdrant：全部正常运行。

真实 Qwen 测试采用 24 条候选，其中 16 条类别确定、8 条需要语义裁决：

| 方案 | 模型请求 | 完成 token | 耗时 |
|---|---:|---:|---:|
| 旧版完整格式化 | 2 批 | 约 980 | 7.88–8.08 秒 |
| 新版语义裁决 | 1 批 | 126 | 1.06–1.13 秒 |

在这项功能针对的混合候选场景中，模型处理时间下降约 86%。若旧版关闭格式化模型，它会更快但直接丢弃不确定候选，二者不具备相同召回能力。

## 能力边界

本次已实现确定性类别分流、源码锚定和规则过滤前置。完整的 AST/调用图证据校验尚未前置到语义分流阶段，仍按《待改进.md》中的第二阶段证据校验项目继续建设。
