# 待改进

以下项目不属于本次“解析能力修复 + 动态 token”提交范围，建议按优先级独立实施和验收。

## P0：准确度与可靠性

1. **第二阶段证据校验**：在最终结论前增加 AST/CFG、定义-使用链、边界条件和资源生命周期验证，减少仅靠语言模型推断产生的误报。
2. **静态分析器融合**：接入 CodeQL、Clang Static Analyzer、Infer 或 cppcheck，把确定性证据作为候选与排序特征，而不是直接拼接全部报告。
3. **跨文件数据流**：在现有调用图之上追踪参数、返回值、全局变量和结构体字段的污点传播，覆盖 source-to-sink 漏洞链。
4. **解析质量门禁指标**：为符号数、函数覆盖率、调用边密度、解析错误率设置基线和异常告警，防止依赖可用但结果再次退化。
5. **标准漏洞评测集**：固定 Recall、Precision、F1、误报类型、证据行命中率，并对每次提示词、模型和检索改动做回归对比。

## P1：吞吐与延迟

1. **检索查询向量缓存**：缓存规范化查询及其 embedding，降低第二阶段重复检索成本。
2. **内容寻址向量去重与回收**：同内容 chunk 复用向量，并为过期 Qdrant point 建立 GC，减少写放大和存储膨胀。
3. **分块器独立版本号**：解析缓存与分块缓存分别版本化，避免仅调整分块逻辑时错误复用旧模板。
4. **Prefix Cache 指标化**：按阶段统计命中 token、命中率和 TTFT，继续固定系统提示词与 schema 前缀，目标命中率不低于 20%。
5. **阶段化 GPU 调度**：Embedding、第一阶段、第二阶段使用有界队列和显存水位控制，减少双模型争用与突发 OOM。
6. **批量与并发自适应**：根据输入长度、剩余显存和队列深度动态选择 embedding batch、`max_num_seqs` 与 `max_num_batched_tokens`。
7. **vLLM 版本专项验证**：升级前后分别验证 V1 Engine、FP8 KV Cache、CUDA Graph、chunked prefill、prefix cache 与 speculative decoding 的兼容组合。

## P2：工程质量与可观测性

1. **缓存并发一致性**：为多 Worker 同时建索引增加幂等 upsert、唯一约束冲突重试和任务级锁。
2. **端到端阶段追踪**：统一记录解析、分块、嵌入、检索、首阶段、二阶段的耗时、token、缓存命中和 GPU 指标。
3. **可复现审计报告**：保存模型版本、提示词版本、解析器版本、索引版本和关键采样参数，支持结果追溯。
4. **生产配置与测试隔离**：测试入口明确禁用根目录生产 `.env`，避免认证与模型开关污染测试结果。
5. **降级策略产品化**：解析器、向量库或模型不可用时，在前端明确显示失败阶段和可重试动作，而不是仅写后台日志。
