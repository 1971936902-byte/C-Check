# 解析能力与动态 Token 优化记录（2026-07-01）

## 本次目标

1. 修复 C 代码解析能力退化，避免解析依赖缺失或旧缓存导致索引质量静默下降。
2. 改进第一阶段动态输出 token 策略，在降低普通代码输出成本的同时保护高风险代码的召回率。

## 解析能力修复

- 将 `tree-sitter` 与 `tree-sitter-c` 纳入后端正式依赖，不再依赖服务器偶然预装。
- 部署脚本安装 `clangd`；生产环境同时检查 tree-sitter-c 与 libclang。
- 解析门禁默认开启：必要解析后端不可用时索引构建明确失败，不再用低质量结果伪装成功。
- libclang 取消 `PARSE_SKIP_FUNCTION_BODIES`，恢复函数体内调用关系抽取。
- 解析缓存版本由 `hybrid-c-parser-v9` 升至 `hybrid-c-parser-v10`，强制淘汰退化期间生成的旧解析/分块缓存。
- 索引统计增加 `parser_degraded`，便于监控和验收。

## 动态 token 策略

第一阶段输出预算由单一行数估算升级为风险感知估算：

```text
预算 = 基础预算
     + 有效代码行数 × 每行权重
     + 函数数量 × 函数权重
     + 危险 API 数量 × 危险操作权重
     + 指针/数组操作数量 × 指针操作权重
```

结果仍受 `candidate_dynamic_min_tokens` 与 `candidate_model_max_tokens` 双重约束。新增权重均可通过环境变量调节：

- `CANDIDATE_DYNAMIC_TOKENS_PER_FUNCTION`
- `CANDIDATE_DYNAMIC_TOKENS_PER_DANGEROUS_OP`
- `CANDIDATE_DYNAMIC_TOKENS_PER_POINTER_OP`

同时读取模型返回的 `finish_reason`。当第一阶段因 `length` 截断时，系统自动执行一次去重续审，避免动态预算偏小时直接损失候选漏洞；续审结果按文件、行号、标题合并去重。

## 三文件实测

测试数据：`imgRead.c`、`imgRead_libfuzzer.c`、`imgRead_socket.c`。

| 文件 | 行数 | 符号/函数 | 调用关系 | 单文件第一阶段预算 |
|---|---:|---:|---:|---:|
| imgRead.c | 134 | 6 | 23 | 1423 |
| imgRead_libfuzzer.c | 87 | 2 | 10 | 1144 |
| imgRead_socket.c | 159 | 7 | 32 | 1576 |
| 合计 | 380 | 15 | 65 | 合并审计 2048（达到保护上限） |

服务器解析后端状态：tree-sitter-c、clangd、libclang 全部可用。相比退化状态下仅获得约 8 个符号的索引，本次直接解析恢复到 15 个函数/符号及 65 条调用关系。

## 测试与部署验收

- 新增解析依赖缺失时快速失败测试。
- 新增同等代码规模下高风险代码预算高于普通代码的测试。
- 新增模型 `finish_reason=length` 元数据与续审基础测试。
- 后端全量测试：`201 passed`，3 条既有框架弃用警告。
- 解析与模型路由专项测试：`93 passed`。
- 截断后自动续审与候选合并专项测试：`1 passed`。
- 生产服务：API、Celery Worker、Jina、Qwen 均为 `active`。
- API 冒烟：`/openapi.json` 返回 HTTP 200。

测试时需隔离仓库根目录的生产 `.env`，否则真实管理员密码和 Mock 配置会污染认证/默认值测试；测试结束后已恢复原配置。
